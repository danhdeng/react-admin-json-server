# react-admin-json-server

# to setup the server

npm install

#to setup the client

cd client
npm install

# to run the project

go back to the parnet folder and run 

npm run dev

to concurrently run both server and client.
